pkg update -y && pkg install python -y
pip install cloudscraper requests beautifulsoup4 aiohttp loguru undetected-playwright playwright --break-system-packages

# Only if you want full browser mode
playwright install chromium

pip install playwright undetected-playwright

run it in a .env
pip install requests 
pip install --upgrade pip

run script kArmA_socialmediaSCrape.py

mode in l0v3 by kArmaSec 
for my Anonymous & LulZSec friends🎩🎭👽🚀

#weareLegion
